#include <iostream>
using namespace std;
int main()
{
    int Int = 3;
    cout << Int;
    // a f j s I Y alphabets
    // 1 5 4 3  Numbers
    // ` ~ ! @ # $ % ^ & * ( ) ; : / ? < > , . 
    // \ | { }  special characters
}